<h1>Songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li>
      <a href="<%= song_path(song) %>">
        <%= link_to song.artist_name, artist_path(@song.artist) if @song.artist %>
        -
        <%= song.title %>
      </a>
    </li>
  <% end %>
</ul>
